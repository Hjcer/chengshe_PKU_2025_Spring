#include<map>
#include<iostream>
#include<string>
#include<vector>

using namespace std;



int main(void)
{
    int n, m, k;
    cin >> n >> m >> k;
    vector<vector<vector<vector<int>>>>miaomiao(n, vector<vector<vector<int>>>(m, vector<vector<int>>(k, vector<int>(2, 0))));

    
    
}